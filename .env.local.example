# FlowSight Tasks — copy to .env.local (never commit .env.local)
# Use the **same** Supabase project URL and anon key as FlowSight_landing so users, teams, and rows match.

NEXT_PUBLIC_SUPABASE_URL=https://your-project-ref.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Optional: canonical site for OG URLs when deploying this app on its own host
# NEXT_PUBLIC_SITE_URL=http://localhost:3001
