export function AutomationBadge() {
  return (
    <span className="inline-flex items-center gap-2 rounded-full border border-primary-teal/30 bg-primary-teal/10 px-3 py-1 text-xs font-semibold uppercase tracking-wide text-primary-teal">
      Auto rollup
    </span>
  )
}
